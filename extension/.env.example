# Base URL of the Apollon backend (the Raspberry Pi). Must also be reflected in
# the "manifest.host_permissions" list in package.json, since that list is
# static and cannot read this variable.
PLASMO_PUBLIC_API_URL=http://localhost:3000
